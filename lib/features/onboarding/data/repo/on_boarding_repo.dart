import 'package:flutter/material.dart';

class OnBoardingRepo extends StatelessWidget {
  const OnBoardingRepo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('OnBoardingRepo')));
  }
}
